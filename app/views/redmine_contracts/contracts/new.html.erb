<%= title l(:label_redmine_contract_new) %>

<%= labelled_form_for @contract,
                      url: { controller: 'redmine_contracts/contracts', action: 'create', project_id: @project } do |f| %>
  <%= render partial: 'form', locals: { f: f } %>
  <%= submit_tag l(:button_create) %>
<% end %>
